<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Безопасный режим модулей
    |--------------------------------------------------------------------------
    |
    | Запрещает полное удаление модуля (откат миграций + удаление файлов).
    | В этом режиме модуль можно только отключить. Защищает от случайной
    | потери данных на боевом сервере.
    |
    */

    'safe_mode' => env('MODULES_SAFE_MODE', false),

    /*
    |--------------------------------------------------------------------------
    | Кэш реестра модулей
    |--------------------------------------------------------------------------
    |
    | Время жизни кэша ответа реестра в секундах. Пока кэш свежий,
    | реестр не опрашивается повторно.
    |
    */

    'registry_cache_ttl' => 3600,

    /*
    |--------------------------------------------------------------------------
    | Максимальный размер скачиваемого модуля
    |--------------------------------------------------------------------------
    |
    | Лимит в мегабайтах для zip-архива модуля, скачиваемого по URL.
    |
    */

    'download_max_size' => 10,

    /*
    |--------------------------------------------------------------------------
    | Реестры по умолчанию
    |--------------------------------------------------------------------------
    |
    | Официальные реестры, добавляются сидером при установке движка.
    |
    */

    'default_registries' => [
        [
            'url'  => 'https://github.com/visavi/rotor-modules/releases/download/registry/registry.json',
            'name' => 'Official Rotor Modules',
        ],
        [
            'url'  => 'https://github.com/visavi/rotor-languages/releases/download/registry/registry.json',
            'name' => 'Official Rotor Languages',
        ],
    ],
];
